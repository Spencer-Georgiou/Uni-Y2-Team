# Flask environment values used in python-dotenv

FLASK_APP=src/api.py
# turn on debug mode
FLASK_DEBUG=1
# set which host the flask runs on
FLASK_RUN_HOST="localhost"
# set which port the flask runs on
FLASK_RUN_PORT=5000
