/**
 * A function returns the sum of two numbers.
 *
 * @param {number} a an addend
 * @param {number} b an addend
 * @returns {number} the sum of two addends
 *
 */
function demoSum (a, b) {
  return a + b
}

module.exports = demoSum
